class Solution {
public:
    int singleNumber(vector<int>& nums) {
        /*
        sort(nums.begin(), nums.end());//The complexity of sort() in c++ is O(nlogn)
        for (int i = 0; i < nums.size(); i+=2){
            if (nums[i]!=nums[i+1])
                return nums[i];
        }
        */
        int answer = 0;
        for (int i = 0; i < nums.size(); i++){
            answer ^= nums[i]; //xor operation: x^x=0, x^0=x
        }
        return answer;
    }
};