class Solution {
public:
    string reverseString(string s) {
        string str = s;
        int length = s.size();
        for (int i = length - 1; i >= 0; i--)
        {
            str[length - 1 - i] = s[i];
        }
        return str; 
    }
};