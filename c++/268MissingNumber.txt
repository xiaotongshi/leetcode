class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int m[nums.size()+1] = {0};
        for (int i = 0; i < nums.size(); i++) m[nums[i]]++;
        for (int i = 0; i < nums.size() + 1; i++) {
            if (m[i] == 0) return i;
        }
    }
};