class Solution {
public:
    int hammingWeight(uint32_t n) {//unit32_t typedef 定义一个32进制的int类型
    int count = 0;
        while(n)
        {
            if(n & 1)
            {
                ++count; 
            }
            n >>= 1;//右侧溢出舍弃
        }
        return count;
    }
};