class Solution {
public:
    int getSum(int a, int b) {
        if(b==0) return a;
        if(a==0) return b;
        int sum = a ^ b; //求和
        int carry = (a & b) << 1; //求进位
        return getSum(sum, carry);
    }
};